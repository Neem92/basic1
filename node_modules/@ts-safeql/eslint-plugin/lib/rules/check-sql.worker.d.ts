import { GenerateError, GenerateResult } from "@ts-safeql/generate";
import { DatabaseInitializationError, InternalError, InvalidMigrationError, InvalidMigrationsPathError, ParsedQuery } from "@ts-safeql/shared";
import { RuleOptionConnection } from "./check-sql.rule";
export interface WorkerParams {
    connection: RuleOptionConnection;
    query: string;
    projectDir: string;
    pgParsed: ParsedQuery.Root;
}
export type WorkerError = InvalidMigrationsPathError | InvalidMigrationError | InternalError | DatabaseInitializationError | GenerateError;
export type WorkerResult = GenerateResult;
